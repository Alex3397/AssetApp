import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    config: {
      flex: 1,
      alignItems: 'center',
      justifyContent: 'center',
    },
    header: {
      alignItems: 'center',
      justifyContent: 'center',
      marginTop: 30,
      fontSize: 20,
    },
    field: {
    },
    button: {
    }
  });